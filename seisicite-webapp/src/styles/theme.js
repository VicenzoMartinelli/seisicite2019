import { createMuiTheme } from '@material-ui/core/styles';

export default createMuiTheme({
  palette: {
    primary: { light: '#ff928b', main: '#ff928b', dark: '#ff928b' },
    secondary: { light: '#20cad6', main: '#20cad6', dark: '#20cad6' }
  }
});